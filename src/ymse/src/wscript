#!/bin/env python
# -*- coding: utf-8 -*-

def build(bld):
	sources = [
		'fileutil.cpp',
		'geometry_intersection.cpp',
		'rect.cpp',
	]

	obj = bld.stlib(
		source=sources,
		target='ymse',
		use=[
			'linear-algebra',
		],
		includes=['../ymse'],
		export_includes=['..'],
	)
