#!/bin/env python
# -*- coding: utf-8 -*-

def build(bld):
	sources = [
		'core_factory.cpp',
		'fileutil.cpp',
		'geometry_intersection.cpp',
		'gl_box_reshaper.cpp',
		'rect.cpp',
		'sdl_core.cpp',
	]

	obj = bld.stlib(
		source=sources,
		target='ymse',
		use=[
			'linear_algebra',
			'game-loop',
			'gl',
			'glew',
			'SDL',
		],
		includes=['../ymse'],
		export_includes=['..'],
	)
